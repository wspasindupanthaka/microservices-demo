spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: file://D:/Courses/Event driven microservices  Spring boot kafka and elastic/My/microservices-demo/config-server-repository
          clone-on-start: false
    fail-fast: true